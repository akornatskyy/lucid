local set_error = require 'validation.mixins.set_error'
local describe, it, assert = describe, it, assert

describe('validation.mixins.set_error', function()
	it('', function()
		assert.not_nil(set_error)
	end)
end)
