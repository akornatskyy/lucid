local validation = require 'validation.mixins.validation'
local describe, it, assert = describe, it, assert

describe('validation.mixins.validation', function()
	it('', function()
		assert.not_nil(validation)
	end)
end)
