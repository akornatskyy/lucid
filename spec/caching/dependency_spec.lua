local dependency = require 'caching.dependency'
local describe, it, assert = describe, it, assert

describe('caching.dependency', function()
	it('', function()
		assert.not_nil(dependency)
	end)
end)
