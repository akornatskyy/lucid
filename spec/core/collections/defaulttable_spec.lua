local defaulttable = require 'core.collections.defaulttable'
local describe, it, assert = describe, it, assert

describe('core.collections.defaulttable', function()
	it('', function()
		assert.not_nil(defaulttable)
	end)
end)
