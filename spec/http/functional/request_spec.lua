local request = require 'http.functional.request'
local describe, it, assert = describe, it, assert

describe('http.functional.request', function()
	it('', function()
		assert.not_nil(request)
	end)
end)
