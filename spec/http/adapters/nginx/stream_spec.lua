local stream = require 'http.adapters.nginx.stream'
local describe, it, assert = describe, it, assert

describe('http.adapters.nginx.stream', function()
	it('', function()
		assert.not_nil(stream)
	end)
end)
