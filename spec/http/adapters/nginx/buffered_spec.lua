local buffered = require 'http.adapters.nginx.buffered'
local describe, it, assert = describe, it, assert

describe('http.adapters.nginx.buffered', function()
	it('', function()
		assert.not_nil(buffered)
	end)
end)
