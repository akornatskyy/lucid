local routing = require 'http.middleware.routing'
local describe, it, assert = describe, it, assert

describe('http.middleware.routing', function()
	it('', function()
		assert.not_nil(routing)
	end)
end)
