local authorize = require 'http.middleware.authorize'
local describe, it, assert = describe, it, assert

describe('http.middleware.authorize', function()
	it('', function()
		assert.not_nil(authorize)
	end)
end)
