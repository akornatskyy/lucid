local cipher = require 'security.crypto.cipher'
local describe, it, assert = describe, it, assert

describe('security.crypto.cipher', function()
	it('', function()
		assert.not_nil(cipher)
	end)
end)
