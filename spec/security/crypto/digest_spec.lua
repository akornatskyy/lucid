local digest = require 'security.crypto.digest'
local describe, it, assert = describe, it, assert

describe('security.crypto.digest', function()
	it('', function()
		assert.not_nil(digest)
	end)
end)
